// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://niedigaming-ctrl.github.io',
  base: '/simracehub-astro',
  output: 'static',
});
